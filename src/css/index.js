import { StyleSheet } from "react-native";

export const globalCss = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  input: {
    height: 60,
    width: "95%",
    margin: 12,
    borderWidth: 1,
    borderRadius: 6,
    padding: 8,
    fontSize: 18,
  },
});
